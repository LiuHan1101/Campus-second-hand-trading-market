<!-- pages/me/profile/profile.wxml -->
<view class="profile-container" style="width: 724rpx; display: block; box-sizing: content-box">
  <!-- 头部用户信息模块 -->
  <view class="header-section card" style="height: 335rpx; display: flex; box-sizing: border-box; position: relative; left: -2rpx; top: -20rpx">
    <view class="user-basic-info">
      <image class="user-avatar" src="{{userInfo.avatar}}" mode="aspectFill" style="width: 139rpx; height: 139rpx; display: block; box-sizing: border-box"></image>
      <view class="user-text-info">
        <view class="user-nickname">{{userInfo.nickname}}</view>
        <view class="user-college">{{userInfo.college}}</view>
        <view class="user-stats">
          <text class="stat-item" style="position: relative; left: -154rpx; top: 47rpx; width: 127rpx; height: 50rpx; display: block; box-sizing: border-box">加入 {{userInfo.joinDays}} 天</text>
          <text class="stat-item" style="position: relative; left: -300rpx; top: 97rpx; width: 127rpx; height: 52rpx; display: block; box-sizing: border-box">信用 {{userInfo.creditScore}}</text>
        </view>
      </view>
    </view>
    <button class="message-btn" bindtap="onSendMessage" style="width: 99rpx; height: 58rpx; display: block; box-sizing: border-box; left: 70rpx; top: 0rpx; position: relative">私信</button>
  </view>

  <!-- 评价模块 -->
  <view class="rating-section card" style="width: 727rpx; height: 331rpx; display: block; box-sizing: border-box; position: relative; left: -6rpx; top: -27rpx">
    <view class="rating-title">用户评价</view>
    <view class="rating-content">
      <!-- 左边评论滚动区域 (70%) -->
      <view class="comment-scroll">
        <scroll-view class="comment-list" scroll-y>
          <view class="comment-item" wx:for="{{comments}}" wx:key="id">
            <text class="comment-user">{{item.user}}:</text>
            <text class="comment-text">{{item.content}}</text>
          </view>
        </scroll-view>
      </view>
      <!-- 右边评分区域 (30%) -->
      <view class="score-display">
        <view class="score-number">{{ratingScore}}</view>
        <view class="score-total">/5分</view>
        <view class="rating-stars">
          <text class="star" wx:for="{{5}}" wx:key="*this">⭐</text>
        </view>
        <view class="rating-count">{{commentCount}}条评价</view>
      </view>
    </view>
  </view>

  <!-- 个人简介模块 -->
  <view class="bio-section card">
    
    <view class="bio-content" style="position: relative; left: 147rpx; top: -616rpx; width: 454rpx; height: 75rpx; display: block; box-sizing: border-box">{{userInfo.bio || '无'}}</view>
    <view class="tags-section">
      
      <view class="tags-list">
        <text class="tag" wx:for="{{userInfo.tags}}" wx:key="*this" style="position: relative; left: 139rpx; top: -607rpx; width: 143rpx; height: 60rpx; display: block; box-sizing: border-box">{{item}}</text>
        
      </view>
    </view>
  </view>

  <!-- 出物和许愿信息模块 -->
  <view class="goods-section" style="width: 729rpx; display: block; box-sizing: border-box; height: 4375rpx; position: relative; left: -27rpx; top: -308rpx">
    <!-- 出物信息 -->
    <view class="goods-tabs">
      <view class="tab {{activeGoodsTab === 'published' ? 'active' : ''}}"data-tab="published" bindtap="switchGoodsTab">
        发布 ({{publishedGoods.length}})
      </view>
      <view class="tab {{activeGoodsTab === 'wish' ? 'active' : ''}}" data-tab="wish" bindtap="switchGoodsTab">
        许愿 ({{wishGoods.length}})
      </view>
    </view>

    <!-- 商品列表 -->
    <view class="goods-list">
      <block wx:if="{{activeGoodsTab === 'published'}}">
        <view class="goods-item" wx:for="{{publishedGoods}}" wx:key="id" data-id="{{item.id}}" bindtap="onGoodsTap" style="width: 712rpx; display: flex; box-sizing: border-box; position: relative; left: -11rpx; top: 0rpx">
          <image class="goods-image" src="{{item.image}}" mode="aspectFill" data-index="{{index}}" binderror="onImageError"></image>
          <view class="goods-info">
            <view class="goods-title">{{item.title}}</view>
            <view class="goods-price">
              <text class="price" wx:if="{{item.price > 0}}">¥{{item.price}}</text>
              <text class="swap-tag" wx:if="{{item.transactionType === 'swap' || item.transactionType === 'both'}}">可换物</text>
            </view>
            <view class="goods-desc">{{item.description}}</view>
          </view>
        </view>
        <view class="empty-state" wx:if="{{sellingGoods.length === 0}}">
          <image src="/images/empty-goods.png"></image>
          <text>还没有发布商品哦～</text>
        </view>
      </block>

      <block wx:if="{{activeGoodsTab === 'wishes'}}">
        <view class="wish-item" wx:for="{{wishGoods}}" wx:key="id" bindtap="onWishTap" data-id="{{item.id}}">
          <view class="wish-icon">🎯</view>
          <view class="wish-info">
            <view class="wish-title">{{item.title}}</view>
            <view class="wish-desc">{{item.description}}</view>
            <view class="wish-offer">
              <text wx:if="{{item.maxPrice > 0}}">愿付 ¥{{item.maxPrice}}</text>
              <text wx:if="{{item.expectedItem}}">愿换 {{item.expectedItem}}</text>
            </view>
          </view>
        </view>
        <view class="empty-state" wx:if="{{wishGoods.length === 0}}">
          <image src="/images/empty-wish.png"></image>
          <text>还没有许愿哦～</text>
        </view>
      </block>
    </view>
  </view>
</view>